# qubits/Nanayakkara_2025_ASQ-50-100.yaml
# Generated by Hardware Profile Updater from corpus sample: Nanayakkara_2025_ASQ-50-100
# Generated: 2026-05-27
#
# Device fields — measured (0): none
# Device fields — assumed  (8): T1_us, T2_us, single_qubit_fidelity_pct, two_qubit_fidelity_pct, readout_fidelity_pct, readout_time_ns, single_qubit_gate_time_ns, two_qubit_gate_time_ns
# Material fields — corpus  (1): Tc_K
#
# [MEASURED] fields came directly from the corpus.
# [ASSUMED]  fields use defaults from transmon_baseline_2026.
# [DERIVED]  fields will be computed at estimation time by t1_decomposition.py.
# Material fields with null values fall back through the tier hierarchy
# in t1_decomposition.py — see loss_channel_model_v2-5.md.
# Review assumed fields before using for quantitative analysis.

profile_type: qubits
name: Nanayakkara_2025_ASQ-50-100
description: PtSi on Si (al. et al.) — no device performance data in corpus + 1 material field(s) for T1 decomposition
platform: superconducting
source: "Extracted from corpus: Nanayakkara_2025_ASQ-50-100"

coherence:
  T1_us: 200    # [ASSUMED]  Energy relaxation time (µs)
  T2_us: 300    # [ASSUMED]  Dephasing time (µs)

gates:
  single_qubit_fidelity_pct: 99.9    # [ASSUMED]  Single-qubit gate fidelity (%)
  two_qubit_fidelity_pct: 99.9    # [ASSUMED]  Two-qubit gate fidelity (%) — primary driver of code distance
  readout_fidelity_pct: 99.5    # [ASSUMED]  Readout fidelity (%)
  readout_time_ns: 500    # [ASSUMED]  Readout time (ns)
  single_qubit_gate_time_ns: 20    # [ASSUMED]  Single-qubit gate time (ns)
  two_qubit_gate_time_ns: 200    # [ASSUMED]  Two-qubit gate time (ns)

# Stage 4: raw material properties — feed t1_decomposition.py at estimation time.
# null = not reported in paper; decomposition falls back to class defaults.
materials:
  Tc_K: 0.9    # [MEASURED] Superconducting critical temperature (K)
  Q_TLS_0: null    # [null]    Unsaturated TLS quality factor — preferred over Qi for loss model
  mean_free_path_nm: null    # [null]    Electron mean free path (nm) — determines clean vs dirty vortex limit
  Qi: null    # [null]    Internal quality factor — single-photon preferred, falls back to internal Qi. Q_TLS_0 preferred over Qi for loss model.
  tan_delta: null    # [null]    Surface loss tangent — best available: tan_delta_effective_surface → loss_tangent_interface → loss_tangent_substrate

# Stage 4: device parameters — feed t1_decomposition.py at estimation time.
device:
  f_qubit_GHz: null    # [null]    Qubit operating frequency (GHz) — required for pad TLS calculation

# Stage 4: geometry inputs (FEM-derived) — rarely reported in papers.
# null → class defaults from transmon_analytical_defaults.yaml are used.
surface_participation:
  p_MS_pad: null    # [null]    Qubit pad metal-substrate participation ratio (FEM). Joshi 2026: 1.3e-4
  p_MS_resonator: null    # [null]    Resonator metal-substrate participation ratio (FEM). Default: 8.63e-4

provenance:
  derived_from_sample: Nanayakkara_2025_ASQ-50-100
  source_authors: "Nanayakkara et al., 2025"
  source_journal: unknown (supporting information document)
  film_material: PtSi
  substrate_material: Si
  date_generated: 2026-05-27
  generated_by: generate_qubit_profile.py
  defaults_from: transmon_baseline_2026
  defaults_path: ../material_defaults/transmon_general_defaults.yaml
  measured_fields: []
  assumed_fields:
    - T1_us
    - T2_us
    - single_qubit_fidelity_pct
    - two_qubit_fidelity_pct
    - readout_fidelity_pct
    - readout_time_ns
    - single_qubit_gate_time_ns
    - two_qubit_gate_time_ns
  material_fields:
    - Tc_K
