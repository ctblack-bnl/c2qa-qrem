# qubits/Mo3Al2C_corpus_average.yaml
# Generated by compute_class_defaults.py
# Generated: 2026-05-23
#
# Corpus-average device profile for material class: Mo3Al2C
# Based on 5 samples in records.db
# Minimum threshold for corpus average: N >= 3
#
# [CORPUS AVERAGE] fields: mean computed from corpus, labeled with N.
# [ASSUMED]        fields: insufficient corpus data — baseline default used.
# null material fields: not enough data to average.
#
# Mode 1 use: select this profile in Baby QREM to benchmark
# what a typical Mo3Al2C qubit achieves in the current corpus.
# Mode 2 use: load a partial measurement and fill gaps from this profile.

profile_type: qubits
name: Mo3Al2C_corpus_average
description: "Mo3Al2C — corpus average across 5 samples (2026-05-23)"
platform: superconducting
source: "Corpus average computed from records.db by compute_class_defaults.py"

coherence:
  T1_us: 200    # [ASSUMED]  Insufficient corpus data (N=0 < 3) — using baseline default
  T2_us: 300    # [ASSUMED]  Insufficient corpus data (N=0 < 3) — using baseline default

gates:
  single_qubit_fidelity_pct: 99.9    # [ASSUMED]  Single-qubit gate fidelity (%) — gate engineering, not material
  single_qubit_gate_time_ns: 20    # [ASSUMED]  Single-qubit gate time (ns) — gate engineering, not material
  two_qubit_fidelity_pct: 99.9    # [ASSUMED]  Two-qubit gate fidelity (%) — primary driver of code distance — gate engineering, not material
  two_qubit_gate_time_ns: 50    # [ASSUMED]  Two-qubit gate time (ns) — gate engineering, not material
  readout_fidelity_pct: 99.5    # [ASSUMED]  Readout fidelity (%) — gate engineering, not material
  readout_time_ns: 500    # [ASSUMED]  Readout time (ns) — gate engineering, not material

# Stage 4: corpus-averaged material properties — feed t1_decomposition.py.
# null = insufficient corpus data for this material class.
materials:
  Tc_K: 9    # [CORPUS_AVERAGE — Mo3Al2C, N=5]  Superconducting critical temperature (K)
  tan_delta: null    # [null]  Surface loss tangent — best available variant (see composition in provenance)
  mean_free_path_nm: null    # [null]  Electron mean free path (nm) — determines clean vs dirty vortex limit
  Qi: null    # [null]  Internal quality factor (see composition in provenance)
  Q_TLS_0: null    # [null]  Unsaturated TLS quality factor — rarely reported, left null

# Stage 4: device parameters.
device:
  f_qubit_GHz: null    # [null]  Device-specific — not averaged across corpus

# Stage 4: geometry inputs — device-specific, not material averages.
surface_participation:
  p_MS_pad: null       # [null]  FEM-derived, device-specific
  p_MS_resonator: null # [null]  FEM-derived, device-specific

provenance:
  generated_by: compute_class_defaults.py
  date_generated: 2026-05-23
  derived_material: Mo3Al2C
  n_samples_total: 5
  min_samples_threshold: 3
  defaults_from: transmon_baseline_2026.yaml
  defaults_path: ../material_defaults/Mo3Al2C_material_defaults.yaml
  corpus_composition:
